# Contributing

感谢参与 OmniMail。

1. 从最新 `main` 创建功能分支。
2. 不要提交真实邮箱、授权码、OAuth 客户端密钥、邮件内容或用户数据。
3. 对协议、IPC、解析或存储改动补充测试。
4. 运行 `npm run check` 和 `npm audit`。
5. Pull Request 说明行为变化、安全影响和人工验证方式。

涉及邮件 HTML、IPC、OAuth、凭据存储、附件或外部 URL 的改动均视为安全敏感变更，需要额外审查。
