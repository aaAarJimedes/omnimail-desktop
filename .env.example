# Public desktop OAuth client IDs. Set these before `npm run build` or `npm run dev`.
# They are identifiers, not secrets. Never put account passwords or OAuth tokens here.
OMNIMAIL_GOOGLE_CLIENT_ID=
OMNIMAIL_MICROSOFT_CLIENT_ID=

# Optional developer override.
OMNIMAIL_LOG_LEVEL=info
